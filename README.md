# 共享雨伞网站系统

一个基于Web的共享雨伞管理系统，支持用户登录、借还伞、伞点管理等功能。

## 功能特性

- 🧑‍💼 **用户登录系统**：支持手机号登录
- 📊 **用户数据管理**：查看借还历史
- 📍 **伞点状态管理**：实时显示各伞点雨伞数量
- ☂️ **借还伞功能**：扫码借伞、还伞
- 🔄 **数据同步**：GitHub仓库作为数据存储
- 📱 **响应式设计**：适配各种设备

## 技术栈

- **前端**：HTML, CSS, JavaScript (原生)
- **后端**：Node.js (简单HTTP服务器)
- **数据存储**：GitHub API
- **二维码生成**：QRCode API

## 项目结构

```
├── index.html          # 主页面
├── style.css           # 样式文件
├── app.js              # 核心业务逻辑
├── github-api.js       # GitHub API封装
├── server.js           # 本地服务器
├── qrcodes.html        # 二维码生成页面
├── test.html           # 测试页面
└── README.md           # 项目说明
```

## 本地运行

1. 克隆或下载项目文件
2. 安装Node.js
3. 在项目目录下运行：
   ```bash
   node server.js
   ```
4. 在浏览器中访问：`http://localhost:3000`

## 部署到GitHub Pages

### 方法1：使用GitHub Desktop（推荐）

1. **创建GitHub仓库**：
   - 访问 [GitHub.com](https://github.com) 并登录
   - 点击右上角的 `+` 图标，选择 "New repository"
   - 输入仓库名称（如 `umbrella-sharing-system`）
   - 选择公开或私有，然后点击 "Create repository"

2. **使用GitHub Desktop同步**：
   - 下载并安装 [GitHub Desktop](https://desktop.github.com/)
   - 打开GitHub Desktop，点击 "File" → "Add Local Repository"
   - 选择项目所在目录，点击 "Add Repository"
   - 在 "Current repository" 面板中，点击 "Publish repository"
   - 选择刚才创建的GitHub仓库，点击 "Publish Repository"

3. **启用GitHub Pages**：
   - 访问GitHub仓库页面
   - 点击 "Settings" → "Pages"
   - 在 "Source" 下拉菜单中选择 "main" 分支，点击 "Save"
   - 等待几分钟，GitHub Pages将自动部署您的网站
   - 访问 `https://[your-username].github.io/[repository-name]` 查看部署结果

### 方法2：使用命令行

1. **创建GitHub仓库**：
   - 按照上述步骤在GitHub网站上创建仓库

2. **配置本地仓库**：
   ```bash
   # 添加远程仓库
   git remote add origin https://github.com/[your-username]/[repository-name].git
   
   # 推送到GitHub
   git push -u origin main
   ```

3. **启用GitHub Pages**：
   - 按照上述步骤在GitHub网站上启用Pages

## 配置说明

### GitHub数据存储

系统使用GitHub仓库作为数据存储。需要在 `github-api.js` 中配置：

```javascript
const owner = 'your-github-username';  // GitHub用户名
const repo = 'your-repository-name';   // 仓库名称
const token = 'your-github-token';     // GitHub访问令牌
```

### 生成访问令牌

1. 访问 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成并保存令牌

## 使用说明

1. **用户登录**：输入手机号登录系统
2. **借伞**：扫描伞点二维码或手动输入伞点ID，点击 "借伞"
3. **还伞**：扫描伞点二维码或手动输入伞点ID，点击 "还伞"
4. **管理伞点**：通过GitHub仓库修改 `data/umbrella-points.json` 文件

## 注意事项

- 本项目使用GitHub API作为数据存储，有请求频率限制
- 建议使用个人访问令牌，不要将令牌泄露给他人
- 数据存储在GitHub仓库的 `data/` 目录下

## 开发说明

### 核心文件说明

- `app.js`：包含主要的业务逻辑和UI交互
- `github-api.js`：封装了GitHub API的调用方法
- `index.html`：页面结构和布局
- `style.css`：页面样式

### 扩展功能

- 添加用户注册功能
- 实现实时消息通知
- 增加雨伞损坏报告功能
- 集成支付系统

## 许可证

MIT License

## 联系方式

如有问题或建议，请提交Issue或Pull Request。
